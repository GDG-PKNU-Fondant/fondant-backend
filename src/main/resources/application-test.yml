spring:
  config:
    activate:
      on-profile: test

  cool-sms:
    api-key: test
    api-secret: test
    caller-number: 010

  cors:
    allowed_origins: "http://localhost:3000"

  jwt:
    secret: testtesttesttesttesttesttesttesttest

  security:
    oauth2:
      client:
        registration:
          naver:
            client-id: test
            client-secret: test
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            scope: [name]
          google:
            client-id: test
            client-secret: test
            redirect-uri: http://localhost:8080/login/oauth2/code/google
            authorization-grant-type: authorization_code
            scope: [email]
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri:        https://nid.naver.com/oauth2.0/token
            user-info-uri:    https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
  cloud:
    aws:
      region:
        static: ap-northeast-2
      stack:
        auto: false
      parameterstore:
        enabled: true
      s3:
        images-bucket: test-image-bucket

  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate.ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

portone:
  api-secret: RM4Ug3uFxTHRWu4nguQiJ2PDT8KXrsanBV3CEEWr8rW7Je6TnGtT6EKOnhNbrsYP4iobPe9cEr0vtJuz
  api-base: https://api.sandbox.portone.io
  webhook:
    secret: whsec_q2tykBu9jWmKK0VCZZVMEtLFwcAQgbqxcdKG4prMN9U=
